# MIGRATION_PLAN — من النافذة الثابتة إلى كوكبت الذكاء المالي

**ELAN Group United — Stakeholder Intelligence Cockpit**

هذه الوثيقة توثق تدقيق المستودع قبل الترحيل، وما تم الحفاظ عليه، وبنية الهدف المعتمدة.

---

## 1. تدقيق البنية السابقة (النسخة الثابتة)

| الملف | الدور | القرار |
|---|---|---|
| `index.html` | هيكل دلالي من 12 قسمًا (Hero → CTA)، RTL كامل | أُرشف في `/legacy-static` |
| `assets/css/styles.css` | الهوية البصرية (أخضر سيادي، جرافيت، رمل، ذهبي هادئ) + استجابة + طباعة | أُرشف؛ الرموز اللونية والخطوط رُحّلت إلى `src/app/globals.css` كـ CSS tokens |
| `assets/js/data.js` | كل المحتوى العربي والأرقام في كائن `ELAN_DATA` مهيكل | أُرشف؛ المحتوى القابل لإعادة الاستخدام رُحّل إلى وحدات TypeScript مكتوبة الأنواع في `src/data/` |
| `assets/js/components.js` | دوال عرض (HeroFinancialCard, LayerCard, UseOfFundsCard, RevenueChannelCard, TimelineStep, RiskGovernanceCard, DeepDiveAccordion, CTASection) | أُرشف؛ استُبدل بمكونات React |
| `assets/js/main.js` | ربط البيانات بالـ DOM + كشف تدريجي عبر IntersectionObserver | أُرشف؛ استُبدل بعرض React |
| `README.md` | توثيق النسخة الثابتة | استُبدل بتوثيق Next.js (النسخة القديمة محفوظة في تاريخ git) |
| إعدادات حزم | لا توجد (مشروع ثابت بلا خطوة بناء) | أُنشئت من الصفر (`package.json`, `tsconfig.json`, `next.config.ts`) |
| افتراض النشر | استضافة ثابتة / Vercel static | Next.js على Vercel (بدون إعداد إضافي) |

### المحتوى القابل لإعادة الاستخدام (رُحّل)

- **الإطار المالي المعتمد (الخيار B فقط):** 100 مليون ريال رأس مال دخول، 30% حصة في نافذة التشغيل المحلية حصراً، 517 مليون ريال تقييم Post-money مرجعي للمنظومة الأوسع (مرجع حجمي فقط — لا يُستخدم لإعادة احتساب الحصة)، أفق 15 سنة.
- **توزيع رأس المال (يساوي 100 مليون):** 34 عمليات ومواهب، 18 تأسيس وحوكمة، 16 بيانات وحوسبة محلية، 14 تكامل ميداني، 8 استدامة وأثر، 10 احتياطي مخاطر → `src/data/useOfFunds.ts`.
- **الطبقات الخمس** (عقل الاستكشاف، غرفة الثقة والحوكمة، التشغيل الميداني الذكي، طبقة الاستثمار والتوسع، الاستدامة والأثر) → وُسّعت إلى ثماني طبقات في `src/data/ecosystemLayers.ts`.
- **قنوات الإيراد الخمس** وحالتها «قابل للتحقق عبر العقود والتنفيذ» → ضمن افتراضات النموذج والغرف.
- **سجل المخاطر والاستجابات الحوكمية** → وُسّع إلى ثمانية مخاطر في `src/data/risks.ts`.
- **وحدات التعمق المالي** ونصوص إخلاء المسؤولية → `src/data/dataRoom.ts` و`DisclaimerBlock`.
- **اللوحة اللونية والخطوط** (Alexandria / IBM Plex Sans Arabic / Tajawal + Inter للأرقام) → tokens في `globals.css`.

### ما لم يُرحّل (استُبدل عمداً)

- السرد التسويقي الطولي (Hero نصي، أقسام قصصية): استُبدل بلوحة قيادة مالية كأول شاشة — التوجه الجديد «كوكبت لأصحاب المصلحة» لا «صفحة هبوط».
- منطق DOM اليدوي والكشف التدريجي: استُبدل بمكونات React ومخططات تفاعلية.

## 2. مخاطر الترحيل وكيف عولجت

| الخطر | المعالجة |
|---|---|
| فقدان النسخة العاملة القديمة | نُقلت كاملة إلى `/legacy-static` (تعمل مستقلة بفتح `legacy-static/index.html`) |
| كسر RTL في المخططات والأرقام | الأرقام بخط Inter داخل عناصر `dir="ltr"` معزولة؛ المخططات داخل حاويات LTR مع تسميات عربية |
| جلب الخطوط وقت البناء | الخطوط تُحمّل وقت التشغيل عبر `<link>` مع بدائل نظامية — البناء لا يعتمد على الشبكة |
| تضخم الحزمة بمكتبة مخططات ثقيلة | Recharts فقط، وتُحمّل في مكونات عميل معزولة |
| انزلاق الصياغة المالية (عوائد مضمونة…) | كل نصوص الأرقام تمر عبر بيانات مركزية تحمل وسم «توضيحي / افتراضات أولية قابلة للمراجعة»، و`DisclaimerBlock` إلزامي قرب كل مخرج مالي |

## 3. البنية المعتمدة (الهدف)

```
/src
  /app            layout.tsx (RTL shell) · page.tsx (الكوكبت) · ecosystem/ · rooms/ · data-room/ · governance/
  /components     /layout /dashboard /ecosystem /rooms /risk /data-room /shared
  /data           financialModel.ts · useOfFunds.ts · ecosystemLayers.ts · stakeholderRooms.ts · risks.ts · dataRoom.ts · reviewPath.ts
  /lib            finance.ts · formatters.ts · validators.ts
  /types          finance.ts · ecosystem.ts · stakeholder.ts
/legacy-static    النسخة الثابتة السابقة كما هي
```

## 4. قائمة تحقق الترحيل

- [x] أرشفة النسخة الثابتة دون حذف (`/legacy-static`)
- [x] توثيق ما يُرحّل وما يُستبدل (هذه الوثيقة)
- [x] تهيئة Next.js App Router + TypeScript + بنية `src/`
- [x] ترحيل الرموز البصرية إلى CSS tokens
- [x] طبقة بيانات مالية مكتوبة الأنواع (سيناريوهات 15 سنة، الخيار B فقط)
- [x] الكوكبت المالي كأول شاشة مع تبديل سيناريوهات يحدّث المؤشرات والمخططات
- [x] طبقات الإيكوسيستم الثماني تفاعلية
- [x] غرف أصحاب المصلحة الست
- [x] بوابة غرفة البيانات (وثائق مقفلة «يتطلب صلاحية وصول»)
- [x] المخاطر والحوكمة + مسار المراجعة الرسمي
- [x] إخلاء المسؤولية قرب كل مخرج مالي
- [x] `npm install` و`npm run build` يمران دون أخطاء
- [x] README محدّث (تشغيل، بناء، نشر، ملاحظة الترحيل)

> جميع الأرقام في المشروع توضيحية ومشتقة من نموذج عمل أولي وخاضعة للمراجعة المالية والقانونية والتشغيلية المستقلة، ولا تمثل ضماناً أو عرضاً ملزماً أو دعوة استثمار عامة.
